The platform's collection schema, vendored so the CLI can check its own
field table against it. This file is DATA, not the source of truth -- the source
is the platform repo.

source:   https://github.com/stone-age-io/platform/blob/main/schema.json
platform release: v0.8.0
platform: repo at d74eb35
schema.json last changed in platform commit: 0f73b37, which added the tenant
          activity feed (a per-organization record of who changed what, readable
          by every role) and audit_logs.changed_fields
copied:   2026-09-19

Refresh with:
  cp ../platform/schema.json cmd/testdata/schema.json
